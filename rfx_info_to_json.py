import os
# import arcpy ## installing Arcpy requires Pro to be installed 

# dump out to json file


if not os.path.exists("rfx_info"):
    os.mkdir("rfx_info")

## arcgis version (Product name, version number)
## raster functions45
import json
